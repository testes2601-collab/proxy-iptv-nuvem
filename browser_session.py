from config import CHROME\_HEADERS try: from curl\_cffi import requests HAS\_CURL\_CFFI = True except ImportError: import requests HAS\_CURL\_CFFI = False def criar\_sessao\_chrome(): """Cria uma sessão HTTP que simula o Google Chrome 122 real para evitar bloqueios de rede da Cloudflare na nuvem. """ if HAS\_CURL\_CFFI: session = requests.Session(impersonate="chrome122") else: session = requests.Session() session.headers.update(CHROME\_HEADERS) return session
